const login = async () => { try { const t = await fetch('http://localhost:8080/api/auth/empleado/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'rekteado@gmail.com', password: 'cachi1234' }) }); const lr = await t.json(); console.log('Token length:', lr.token?.length); const r = await fetch('http://localhost:8080/api/workflows/ai/generate', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + lr.token }, body: JSON.stringify({ politicaNegocio: 'tramite' }) }); console.log('Generate Status:', r.status); console.log(await r.text()); } catch(e) { console.error(e); } }; login();
